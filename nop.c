#include "monty.h"
/**
 * nopf - doesn't do anything
 * @stack: A double pointer to the stack (top node of stack)
 * @line_number: line number of input file
 */
void nopf(stack_t **stack, unsigned int line_number)
{
	(void)(stack);
	(void)(line_number);
}
